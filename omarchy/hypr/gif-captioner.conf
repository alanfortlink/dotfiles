# gif-captioner popup styling. tag +floating-window → float + center, then
# size override gives the gif grid + preview room to breathe.
windowrule = tag +floating-window, match:class (net.alanfortlink.gif_captioner)
windowrule = size 1280 820,        match:class (net.alanfortlink.gif_captioner)
