-- gif-captioner popup styling. tag +floating-window -> float + center, then
-- size override gives the gif grid + preview room to breathe.
o.window("(net.alanfortlink.gif_captioner)", {
  tag = "+floating-window",
  size = "1280 820",
})
