#!/bin/bash
# Pop an input prompt and launch claude with the typed text in a new terminal.
set -euo pipefail

prompt=$(omarchy menu input "Ask Claude…" 2>/dev/null) || exit 0
[ -z "$prompt" ] && exit 0

setsid uwsm-app -- ghostty --class=claude-ask -e claude "$prompt" >/dev/null 2>&1 &
